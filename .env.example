# Production values are managed in the Sites environment settings.
CONTACT_TO_EMAIL=founder@example.com

# Optional professional delivery provider. The form uses an interim fallback
# until this key is configured.
RESEND_API_KEY=
CONTACT_FROM_EMAIL=DataPillars Website <website@datapillars.ae>
